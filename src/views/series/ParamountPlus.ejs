<!--HEAD-->
<%- include(".././partials/headNoticias") %>

    <body class="bg1">
        <!--HEADER-->
        <%- include(".././partials/header") %>

            <main>
                <!--EJS-->
                <h1 class="titulo4">series Paramount+</h1>

                <div class="row row-cols-1 row-cols-md-3">
                    <% for (let i=0; i < seriesParamountPlus.length; i++) { %>
                        <div class="col mb-4">
                            <div class="card-body">
                                <div class="peliculas-container">
                                    <div class="pelicula">
                                        <a href="./seriesParamountPlus/detalle/<%=seriesParamountPlus[i].id%>">
                                            <img class="imagen" src="<%= seriesParamountPlus[i].imagen1 %>"
                                                alt="<%= seriesParamountPlus[i].nombre %>" width="470px">
                                        </a>
                                        <div class="textoPeliculas">
                                            <a href="./seriesParamountPlus/detalle/<%=seriesParamountPlus[i].id%>" class="textoenlasimagenes">
                                              <%= seriesParamountPlus[i].nombre %>
                                            </a>
                                          </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% } %>
                </div>
            </main>

            <!-- FOOTER -->
            <%- include(".././partials/footer.ejs") %>
    </body>

    </html>